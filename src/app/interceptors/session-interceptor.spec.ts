import { SessionInterceptor } from './session-interceptor';

describe('SessionInterceptor', () => {
  it('should create an instance', () => {
    expect(new SessionInterceptor()).toBeTruthy();
  });
});
