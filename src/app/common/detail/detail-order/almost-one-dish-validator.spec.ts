import { AlmostOneDishValidator } from './almost-one-dish-validator';

describe('AlmostOneDishValidator', () => {
  it('should create an instance', () => {
    expect(new AlmostOneDishValidator()).toBeTruthy();
  });
});
