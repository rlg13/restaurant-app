import { FilterOrderParams } from './filter-order-params';

describe('FilterOrderParams', () => {
  it('should create an instance', () => {
    expect(new FilterOrderParams()).toBeTruthy();
  });
});
